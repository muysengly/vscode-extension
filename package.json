{
  "name": "vscode-extension",
  "displayName": "vscode-extension",
  "description": "Send file line references to OpenCode in the terminal.",
  "version": "0.0.1",
  "publisher": "muysengly",
  "icon": "icons/bun.png",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.send"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.send",
        "title": "Extension: Send File Line Range to OpenCode"
      },
      {
        "command": "extension.sendSelected",
        "title": "Extension: Send Selected File/Folder to OpenCode"
      },
      {
        "command": "extension.focus",
        "title": "Extension: Focus OpenCode Terminal"
      }
    ],
    "keybindings": [
      {
        "key": "ctrl+o",
        "command": "extension.focus"
      },
      {
        "key": "ctrl+o",
        "command": "extension.send",
        "when": "editorTextFocus"
      },
      {
        "key": "ctrl+o",
        "command": "extension.sendSelected",
        "when": "filesExplorerFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npm run compile && vsce package --out vscode-extension-0.0.1.vsix --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/vscode": "^1.75.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.4.0"
  }
}
